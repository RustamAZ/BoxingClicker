import type { EnemyConfig } from "./types";

export const firstEnemyConfig: EnemyConfig = {
  health_range: [18, 35],
  damage_range: [4, 7],
  attack_speed_range: [0.35, 0.55],
  xp_reward: 1,
  buff_container_reward: 8,
  lootbox_container_reward: 8,
  emerald_drop_chance: 0.08,
};
